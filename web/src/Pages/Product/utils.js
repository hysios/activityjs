import React, {Component} from 'react'
import styled, { css } from 'react-emotion'

export const Currency = styled('p')`
    font-size: 1.5rem;
`

export const Heading = (props) => (<h1>{props.title} </h1>)
